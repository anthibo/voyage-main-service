export interface CustomizedTripDTO{
    cityId: string;
    name: string;
}
export interface GeneratedTripDTO{
    numberOfDays: number;
    activities: string[];
    cityId: string;
    name: string;

}